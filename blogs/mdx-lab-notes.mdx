export const meta = {
  title: 'MDX Lab Notes: Building Safer Security Writeups',
  date: '2026-04-27',
  tags: ['mdx', 'security', 'workflow'],
  excerpt: 'A sample MDX post with JSX callouts, code blocks, and media to test rich post rendering.',
  slug: 'mdx-lab-notes',
  searchText: 'mdx jsx callout secure writeups markdown components'
}

export const panelStyle = {
  border: '1px dashed #beb094',
  background: '#fff8ea',
  padding: '0.65rem 0.75rem',
  margin: '0.8rem 0'
}

# MDX Lab Notes

This post exists as a **sample MDX entry** so the blog can render JSX and markdown together.

<div style={panelStyle}>
  <strong>Why this matters:</strong> MDX lets you mix narrative and interactive components in security writeups.
</div>

## Example: Structured Vulnerability Summary

- Scope: Internal web app staging environment
- Test style: Authenticated manual review + targeted automation
- Result: 2 high, 1 medium, 3 low findings

```bash
# Repro baseline setup
npm ci
npm run dev
```

## Embedded Image Test

<img src="/favicon.svg" alt="Theme favicon sample" width="72" />

## Next Step Checklist

1. Add custom components for findings severity badges.
2. Add a reusable timeline component for incident response reports.
3. Add diagrams for attack paths in future MDX posts.
